package com.frontierapp.frontierapp;

public interface PartnerWriter {
    Boolean addPartner(Profile profile);
    Boolean updatePartner(Profile profile);
    Boolean deletePartner(Profile profile);
}
